version: 2.1

description: >
  Use Shipyard with CircleCI to run CircleCI jobs on Staging Environments automatically deployed by Shipyard, authenticating into them via a bypass token instead of oauth.

display:
  home_url: "https://shipyard.build/"
  source_url: "https://github.com/shipyardbuild/circleci-orb"

orbs:
  jq: circleci/jq@2.2.0
